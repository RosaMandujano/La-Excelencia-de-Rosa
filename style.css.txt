body {
    margin: 0;
    font-family: "Poppins", Arial, sans-serif;
    color: #2b2b2b;
    background: #fffaf5;
}

/* HERO SECTION */
.hero {
    position: relative;
    height: 90vh;
    background: url('https://i.imgur.com/NXj6i9X.jpg') center/cover no-repeat;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
}

.hero .overlay {
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(178, 40, 45, 0.55);
}

.hero-content {
    position: relative;
    z-index: 2;
    color: white;
}

.hero-content h1 {
    font-size: 3rem;
    letter-spacing: 2px;
}

.hero-content p {
    font-size: 1.3rem;
    margin-bottom: 20px;
}

.btn {
    background: #dfa53a;
    padding: 12px 25px;
    color: white;
    text-decoration: none;
    border-radius: 4px;
    font-weight: bold;
}
.btn:hover {
    background: #c78d29;
}

/* SECTIONS */
section {
    padding: 60px 20px;
    text-align: center;
}

.about, .order {
    max-width: 750px;
    margin: auto;
}

/* MENU */
.menu-box {
    background: white;
    max-width: 600px;
    margin: 30px auto;
    padding: 25px;
    border-radius: 8px;
    border-left: 5px solid #b3282d;
    box-shadow: 0 3px 10px rgba(0,0,0,0.1);
}

.menu ul {
    list-style: none;
    padding: 0;
}

.menu li {
    padding: 8px 0;
    border-bottom: 1px solid #ddd;
}

/* FOOTER */
.footer {
    background: #2b2b2b;
    color: white;
    padding: 15px;
    text-align: center;
}
